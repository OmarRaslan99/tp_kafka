[project]
name = "tp-kafka"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "avro>=1.12.1",
    "kafka-python>=3.0.0",
]
